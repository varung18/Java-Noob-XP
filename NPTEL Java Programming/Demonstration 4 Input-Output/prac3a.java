public class prac3a {
    public static void main (String args[]) {
        int i = 0;
        i = Integer.parseInt(args[0]);
        System.out.println("The value of i in Integer is " + i);
    }// Its a program where we have to enter the integers and it will return it back
    // But if we give input of something else, which is not integer. Then it will throw an error.
}
