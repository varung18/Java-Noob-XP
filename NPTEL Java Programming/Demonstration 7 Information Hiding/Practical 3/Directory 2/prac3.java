// Example of default access modifier


class prac3 {
    public static void main (String args[]){
        A obj = new A();
        obj.msg();
    }
}

// If two files are stored in different directories,
// then compiler will throw and error, saying that A is not found