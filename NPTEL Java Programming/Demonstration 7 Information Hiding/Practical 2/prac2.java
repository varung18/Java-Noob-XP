// Example of default access modifier


class prac2 {
    public static void main (String args[]){
        A obj = new A();
        obj.msg();
    }
}

// If two files are stored in th esame directory, so the resolution is not a
// problem